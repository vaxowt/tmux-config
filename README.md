# tmux config

## 安装

克隆到 `${XDG_CONFIG_HOME}/tmux`

安装插件管理器 `tpm-redux`：

```bash
git clone https://github.com/RyanMacG/tpm-redux.git ${XDG_CONFIG_HOME}/tmux/plugins/tpm-redux
```

打开 `tmux`，默认 prefix 设置为 `CTRL-a`，使用 `CTRL-a I` 安装插件

使用 `CTRL-a CTRL-h` 查看帮助
